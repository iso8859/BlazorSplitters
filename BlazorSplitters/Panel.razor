@if (Size != "0")
{
    @ChildContent
}

@code {
    [CascadingParameter]
    public TestSplitter Parent { get; set; }
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    [Parameter]
    public string Size { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Parent.AddPanel(this);
    }
}